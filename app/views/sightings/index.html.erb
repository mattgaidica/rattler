<p id="notice"><%= notice %></p>

<h1>Sightings</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Squirrel</th>
      <th>Location</th>
      <th>Project</th>
      <th>Sighted At</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sightings.each do |sighting| %>
      <tr>
        <td><%= sighting.name %></td>
        <td><%= sighting.squirrel_id %></td>
        <td><%= sighting.location_id %></td>
        <td><%= sighting.project_id %></td>
        <td><%= sighting.sighted_at %></td>
        <td><%= link_to 'Show', sighting %></td>
        <td><%= link_to 'Edit', edit_sighting_path(sighting) %></td>
        <td><%= link_to 'Destroy', sighting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sighting', new_sighting_path %>
